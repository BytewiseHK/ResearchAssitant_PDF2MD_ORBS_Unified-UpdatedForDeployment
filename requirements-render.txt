# Slim dependencies for Render when using MinerU.net cloud PDF extraction
# (PDF_EXTRACT_BACKEND=cloud). No local PyTorch / MinerU pipeline.
# Docker: build with --build-arg REQUIREMENTS_FILE=requirements-render.txt

# Core
fastapi==0.116.1
uvicorn==0.35.0
starlette==0.47.2
pydantic==2.11.7
pydantic-settings==2.10.1

# API clients
openai==1.97.0
requests==2.32.4
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0

# Uploads
python-multipart==0.0.20

# Utilities
python-dotenv==1.1.1
jiter==0.10.0
psutil==6.0.0
